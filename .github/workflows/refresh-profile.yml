name: Refresh profile metrics

on:
  push:
    branches:
      - main
    paths:
      - scripts/update-profile.mjs
      - .github/workflows/refresh-profile.yml
  schedule:
    - cron: "23 1 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: profile-metrics
  cancel-in-progress: true

jobs:
  refresh:
    name: Build profile cards
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Check out profile repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Generate metrics
        run: node scripts/update-profile.mjs
        env:
          PROFILE_LOGIN: ${{ vars.PROFILE_LOGIN || github.repository_owner }}
          PROFILE_TOKEN: ${{ secrets.PROFILE_TOKEN }}
          REQUIRE_COMPLETE_REPOSITORY_ACCESS: "true"

      - name: Commit changed cards
        shell: bash
        run: |
          if git diff --quiet -- assets/profile-stats-light.svg assets/profile-stats-dark.svg assets/languages-light.svg assets/languages-dark.svg; then
            echo "Profile metrics are already current."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add assets/profile-stats-light.svg assets/profile-stats-dark.svg assets/languages-light.svg assets/languages-dark.svg
          git commit -m "chore(profile): refresh profile cards"
          git push
